# Cached lightning data — can be re-fetched from NCEI SWDI
# Leading slash anchors this to the repo root so src/data/ is NOT excluded
/data/

# Python
__pycache__/
*.py[cod]
*.pyo
.env
venv/
.venv/

# Streamlit
.streamlit/secrets.toml
